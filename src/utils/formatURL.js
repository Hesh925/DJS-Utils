/**
 * Fuck this shit
 * @param {string} URL 
 * @returns {string} Formated URL.
 */
module.exports = (URL) => {
    if ((URL.toLowerCase()).startsWith("https://" || "http://")){

    } else {
        URL = `https://`
    }
}